// import { createAction } from '@reduxjs/toolkit'

// const sortByName = createAction('countries/sort by name')

// let action = increment()
// // { type: 'counter/increment' }

// action = increment(3)
// // returns { type: 'counter/increment', payload: 3 }